schema-version: 1.0

type: system

system:
  user: ubuntu
  container: "{% xbeeName %}:{% xbeeVersion %}"

dev: ./ubuntu-dev

provider:
  virtualbox:
      disk: http://cloud-images.ubuntu.com/bionic/current/bionic-server-cloudimg-amd64.vmdk
  aws:
    amis:
      linux_amd64:

    
in:
  timezone: Europe/Paris
 
provision:
  - env: TZ={% timezone %}
