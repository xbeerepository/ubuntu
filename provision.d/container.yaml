labels:
  container: true
actions:
  - pkg:
      name: ca-certificates wget
      update: true
    after:
        cmd: apt-get install -y tzdata
        envs:
          - DEBIAN_FRONTEND=noninteractive


