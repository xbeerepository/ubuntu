labels:
  container: true
actions:
  - pkg:
      name: ca-certificates wget
      update: true
    before: "{{ .templates }}/timezone.sh"
    after:
        cmd: apt-get install -y tzdata
        envs:
          - DEBIAN_FRONTEND=noninteractive


